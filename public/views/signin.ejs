<!DOCTYPE html>
<html>

<head>
	<title>Sign In Page</title>
	
	<style type="text/css" media="screen">
		body{
			height:97.5vh;
			background:
			linear-gradient(to bottom right,transparent 50%,rgb(124, 220, 124) 50%) bottom/180% 110% no-repeat,
			linear-gradient(97deg, white 0%, white 80%) top/100% 100% no-repeat;
		}
		#main-cont{
			background-color: rgb(210, 255, 210);
			position: absolute;
			height: 340px;
			width: 400px;
			max-width: 450px;
			text-align: center;
			font-size: 30px;
			color: darkgreen;
			font-family: Arial, Helvetica, sans-serif;
			top: 50%;
			left: 50%;
			transform: translate(-50%, -50%);
			border: none;
			border-radius: 4px;
			overflow: hidden;
		}
		#app-title{
			background-color: rgba(206, 255, 206, 0.25);
			color: green;
		}
		#phrase{
			color: green;
			background-color: white;
			padding-top: 5px;
			height: 25px;
			font-size: 20px;
			margin-top: -20px;
		}
		.inputs{
			background-color: rgba(206, 255, 206, 0.25);
			height: 40px;
			width: 90%;
			padding-left: 10px;
			border: none;
			border-radius: 8px;
			margin-bottom: 10px;
			box-shadow: 1px 1px black;
			background-color: white;
			outline: none;
		}
		#signinbutton{
			background-color: white;
			border: none;
			border-radius: 4px;
			width: 120px;
			height: 30px;
			float: right;
			margin-top: 20px;
			margin-right: 10px;
			transition: 0.5s;
		}
		#signinbutton:hover{
			color: green;
			cursor: pointer;
		}
		#signuplink{
			color: blue;
		}
		#errormessage{
			color: red;
			background-color: rgba(255, 196, 196, 0.534);
			position: absolute;
			float: left;
			width: 60%;
			margin-left: 12px;
			margin-top: 22px;
			font-size: 15px;
			padding: 5px 5px 5px 5px;
			border-radius: 4px;
		}
		#signuptext{
			font-size: medium;
			margin-left: 10px;
			margin-top: 70px;
			margin-bottom: 20px;
            float: left;
		}
	</style>
</head>

<body>
	<div id = "background-shape"></div>
	<div id = "main-cont"><h3 id = "app-title">TITLE</h3>
		<p id = "phrase">
		<% if (locals.phrase){ %>
			<%=phrase%>
		<% } else { %>
			<p id = "phrase">Let's Get Started</p>
		<% } %>
		</p>
		<div  id = "inputs-cont">
			<form  action = "/signin" method = "POST">
				<input name = "user" class="inputs" type="text" placeholder="Enter Username" required autocomplete="off">
				<input name = "pass" class="inputs" type="password" placeholder="Enter Password" required autocomplete="off">

				<button type="submit" id = "signinbutton">Sign In</button>
					<% if (locals.errormessage){ %>
						<p id = "errormessage">
							<%=errormessage%>
						</p>
					<% } %>
			</form>
		</div>
		<p id = "signuptext">No Account? <a href = "/signup" id = "signuplink">Sign Up Here!</a></p>
	</div>
</body>

</html>
